@model CJCProjectEstimatorMVC.Models.AppUserViewModel
@{
    ViewBag.Title = "Login";
}

    @using (Html.BeginForm("Index", "Login"))
    {
        
          @Html.LabelFor(model => model.UserName, "User Name:")
          @Html.TextBoxFor(model => model.UserName, new { maxlength = 40 })
          @Html.LabelFor(model => model.Password, "Password:")
          @Html.PasswordFor(model => model.Password, new { maxlength = 15 })
          <input name="submit" type="submit" value="Login" id="Submit" />


    }
